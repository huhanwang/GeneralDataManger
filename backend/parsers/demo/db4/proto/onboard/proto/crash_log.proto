
syntax = "proto2";

package snoah;
import "onboard/lite/proto/lite_common.proto";

// Crashlog, log produced when log crashed.
message CrashLogProto {
  optional LiteHeader header = 1;
  optional string module_name = 2;
  repeated string logs = 3;
}
