
syntax = "proto2";

package snoah;

import "onboard/lite/proto/lite_common.proto";

message FTraceProto {
  optional LiteHeader header = 1;
  // Consider compress it when collect it.
  repeated string cpu_raw_trace = 2;
}
