syntax = "proto2";

package snoah.qlfs;

message SchemaProto {
  enum Type {
    MODIFY = 0;
    DELETE = 1;
    ADD = 2;
    KEEP = 3;
  }
  message FileProto {
    optional string path = 1;
    optional string sha = 2;
    optional Type type = 3;
  }
  repeated FileProto files = 1;
}